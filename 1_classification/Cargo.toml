[package]
name = "ml_playground"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.16", features = ["train", "ndarray", "wgpu", "vision"] }
serde = { version = "1", features = ["derive"] }
